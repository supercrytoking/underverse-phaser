

    // var pointer = scene.game.input.activePointer;
    // var threshold = 40;

    // if (scene.swipe.direction) {
    //     scene.physics.moveTo(scene.player, pointer.worldX, pointer.worldY, scene.speed);
    // }
    // if (pointer.isDown) {
    //     console.log(scene.swipe);
    //     // console.log(`Start: ${scene.startX} ${scene.startY}, Current: ${pointer.x} ${pointer.y}`);
    //     if (pointer.x > (scene.startX + threshold)) {
    //         scene.swipe.right = true;
    //         scene.swipe.direction = 'right';
    //     }
    //     if (pointer.x < (scene.startX - threshold)) {
    //         scene.swipe.left = true;
    //         scene.swipe.direction = 'left';
    //     }
    //     if (pointer.y > (scene.startY + threshold)) {
    //         scene.swipe.down = true;
    //         scene.swipe.direction = 'down';
    //     }
    //     if (pointer.y < (scene.startY - threshold)) {
    //         scene.swipe.up = true;
    //         scene.swipe.direction = 'up';
    //     }
    // }